export const META_TICK = 12;
export const TIME_MARGIN = 2;
export const META_THRESHOLD = (META_TICK * 2) + TIME_MARGIN;
export const MIN_TIME_LEFT = (META_TICK / 4);
export const ZERO = 0;
export const TIME_PASSED = -1;
